# Frontend Environment Variables

# API URL for production deployments
# For development: leave empty or use http://localhost:8000
# For production on Railway (with nginx): leave empty (relative paths)
# For production on separate domain: https://express-entry-production.up.railway.app
VITE_API_URL=

# Enable debug logging
VITE_DEBUG=false
